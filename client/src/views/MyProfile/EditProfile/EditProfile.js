import React from "react";

function EditProfile({ setToggle }) {
  return (
    <div>
      EditProfile
      <button onClick={setToggle}>Click me to view Profile</button>
    </div>
  );
}

export default EditProfile;
